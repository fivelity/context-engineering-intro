/// <reference types="svelte" />
import type { Action } from 'svelte/action';
type ScrollIntoViewOptions = {
    condition: boolean | ((node: HTMLElement) => boolean);
    /** Only scroll if needed (not visible in scroll parent).  Similar to non-standard `scrollIntoViewIfNeeded()` */
    onlyIfNeeded?: boolean;
    initial?: boolean;
    delay?: number;
};
export declare const scrollIntoView: Action<HTMLElement, ScrollIntoViewOptions>;
type ScrollShadowOptions = {
    top?: {
        color?: string;
    };
    bottom?: {
        color?: string;
    };
    left?: {
        color?: string;
    };
    right?: {
        color?: string;
    };
};
export declare const scrollShadow: Action<HTMLElement, ScrollShadowOptions>;
type ScrollFadeOptions = {
    length?: number;
    scrollRatio?: number;
};
export declare const scrollFade: Action<HTMLElement, ScrollFadeOptions>;
export {};
