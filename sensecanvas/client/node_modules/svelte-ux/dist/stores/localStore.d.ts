/// <reference types="svelte" />
import type { Expiry } from '../utils/object';
type LocalStoreOptions<Value> = {
    expiry?: Expiry | ((previousExpiry: Expiry | undefined | null) => Expiry);
    override?: Value;
};
declare function localStore<Value>(key: string, initialValue: Value, options?: LocalStoreOptions<Value>): import("svelte/store").Writable<Value>;
export default localStore;
