/// <reference types="svelte" />
import { type Readable, type Subscriber } from 'svelte/store';
declare function dirtyStore<T extends Readable<any>>(store: T): {
    subscribe(run: Subscriber<boolean>): () => void;
    reset(): void;
};
export default dirtyStore;
