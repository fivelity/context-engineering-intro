import { type ComponentProps, SvelteComponent } from 'svelte';
import type * as Components from './';
type ComponentName = keyof typeof Components;
type ClassesProp<T> = T extends {
    prototype: infer PR extends SvelteComponent;
} ? ComponentProps<PR> extends {
    classes?: any;
} ? ComponentProps<PR>['classes'] : never : never;
export type Theme = {
    [key in ComponentName]?: ClassesProp<(typeof Components)[key]> | string;
};
export declare function createTheme(theme: Theme): void;
export declare function getTheme(): Theme;
export declare function getComponentTheme(name: ComponentName): any;
export {};
