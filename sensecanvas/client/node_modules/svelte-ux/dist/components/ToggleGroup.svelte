<script context="module">
  export const groupKey = {};
</script>

<script>import { setContext, createEventDispatcher } from 'svelte';
import { writable } from 'svelte/store';
import { crossfade, fade } from 'svelte/transition';
import { cls } from '../utils/styles';
import Logger from '../utils/logger';
import { getComponentTheme } from './theme';
export let value = undefined; // index or value
export let autoscroll = false;
export let variant = 'default';
export let size = 'md';
export let rounded = variant !== 'underline';
export let gap = false;
export let inset = false;
export let vertical = false;
export let classes = {};
const theme = getComponentTheme('ToggleGroup');
$: variantClasses = {
    default: {
        options: '',
        label: 'text-gray-500 hover:text-accent-500 [&.selected]:text-accent-500',
        indicator: 'h-full bg-accent-50',
    },
    outline: {
        options: 'border hover:border-gray-300',
        label: 'text-gray-500 hover:text-accent-500 [&.selected]:text-accent-500',
        indicator: 'h-full w-full bg-accent-50',
    },
    fill: {
        options: cls(!gap && 'bg-accent-500/10'),
        label: cls('text-accent-500 hover:text-accent-600 hover:bg-accent-500/10 [&.selected]:text-accent-50', gap && 'bg-accent-500/10'),
        indicator: 'h-full bg-accent-500',
    },
    'fill-light': {
        options: cls(!gap && 'bg-gray-500/10'),
        label: cls('text-gray-500 hover:text-gray-600 hover:bg-gray-500/10 [&.selected]:text-accent-500', gap && 'bg-gray-500/10'),
        indicator: 'h-full bg-accent-100',
    },
    'fill-white': {
        options: cls(!gap && 'bg-gray-500/10'),
        label: cls('text-gray-500 hover:text-gray-600 hover:bg-gray-500/10 [&.selected]:text-accent-500', gap && 'bg-gray-500/10'),
        indicator: 'h-full bg-white border',
    },
    underline: {
        options: vertical ? 'border-r' : 'border-b',
        label: 'relative text-black/50 font-bold hover:text-accent-500 hover:bg-accent-500/10 [&.selected]:text-accent-500',
        indicator: cls('absolute border-accent-500', vertical
            ? 'top-0 right-0 h-full border-l-4 rounded-l'
            : 'bottom-0 left-0 w-full border-t-2 rounded-t'),
    },
    none: {},
};
// Toss classes into a store so it can be reactively read from context
const classesStore = writable(classes);
$: $classesStore = {
    root: cls('', variantClasses[variant].root, classes.root),
    options: cls('grid overflow-auto', vertical ? 'grid-flow-row' : 'grid-flow-col', rounded === 'full' ? 'rounded-full' : rounded && 'rounded', gap === true ? 'gap-1' : gap === 'px' ? 'gap-px' : '', inset ? 'p-[2px]' : '', variantClasses[variant].options, theme.options, classes.options),
    label: cls('text-center cursor-pointer', {
        xs: 'text-xs',
        sm: 'text-xs',
        md: 'text-sm',
        lg: 'text-base',
    }[size], rounded === 'full' ? 'rounded-full' : rounded && 'rounded', 
    // If adding gap between options, round first and last outside edges for options and the indicator
    gap &&
        (vertical
            ? [
                '[&:not(:first-child)]:rounded-t-none',
                '[&:not(:last-child)]:rounded-b-none',
                '[&:not(:first-child)_.indicator]:rounded-t-none',
                '[&:not(:last-child)_.indicator]:rounded-b-none',
            ]
            : [
                '[&:not(:first-child)]:rounded-l-none',
                '[&:not(:last-child)]:rounded-r-none',
                '[&:not(:first-child)_.indicator]:rounded-l-none',
                '[&:not(:last-child)_.indicator]:rounded-r-none',
            ]), variantClasses[variant].label, theme.label, classes.label),
    option: cls(variant !== 'none' && 'px-4 font-medium', {
        xs: '',
        sm: 'py-1',
        md: 'py-1',
        lg: 'py-1',
    }[size], variantClasses[variant].option, theme.option, classes.option),
    indicator: cls('z-0', rounded === 'full' ? 'rounded-full' : rounded && 'rounded', variantClasses[variant].indicator, theme.indicator, classes.indicator),
};
const logger = new Logger('ToggleGroup');
const options = [];
const optionsByValue = new Map();
const panels = [];
const selectedOption = writable(undefined);
const selectedPanel = writable(undefined);
const [send, receive] = crossfade({ fallback: fade });
const dispatch = createEventDispatcher();
// Value changed (controlled)
$: {
    // Find selected option element based on selected value/index
    const newSelectedOption = optionsByValue.get(value) || options[value];
    logger.debug('value changed', { value, newSelectedOption });
    selectOption(newSelectedOption, value);
}
function registerOption(option, optionValue) {
    options.push(option);
    optionsByValue.set(optionValue, option);
    // Select option if selected after being registered
    if (optionValue === value) {
        selectOption(option, optionValue);
    }
}
function unregisterOption(option, optionValue) {
    const i = options.indexOf(option);
    options.splice(i, 1);
    selectedOption.update((current) => current === option ? options[i] || options[options.length - 1] : current);
    optionsByValue.delete(optionValue);
}
function registerPanel(panel) {
    panels.push(panel);
    selectedPanel.update((current) => current || panel);
}
function unregisterPanel(panel) {
    const i = panels.indexOf(panel);
    panels.splice(i, 1);
    selectedPanel.update((current) => current === panel ? panels[i] || panels[panels.length - 1] : current);
}
function selectOption(option, optionValue) {
    logger.debug('selectOption', { option, optionValue });
    if (value !== optionValue) {
        dispatch('change', { value: optionValue });
    }
    $selectedOption = option;
    value = optionValue;
    const i = options.indexOf(option);
    $selectedPanel = panels[i];
}
setContext(groupKey, {
    registerOption,
    unregisterOption,
    registerPanel,
    unregisterPanel,
    selectOption,
    selectedOption,
    selectedPanel,
    crossfade: [send, receive],
    classes: classesStore,
    autoscroll,
});
</script>

<div
  class={cls('ToggleGroup', `variant-${variant}`, $classesStore.root, $$props.class)}
  {...$$restProps}
>
  <!-- TODO: Figure out why fieldset and class="overflow-auto" doesn't work  -->
  <div class={cls('options', $classesStore.options)}>
    <slot />
  </div>
  <slot name="panes" />
</div>
